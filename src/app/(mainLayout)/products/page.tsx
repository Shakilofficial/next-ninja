import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Products",
  description: "Products page",
};

const ProductsPage = () => {
  return (
    <div>
      <h1>This is the Products page component</h1>
    </div>
  );
};

export default ProductsPage;
